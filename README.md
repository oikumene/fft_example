# FFT library usage example on FreeBSD

This is just a trivial example of using FFTW and clFFT on FreeBSD.

The Makefile requires FreeBSD's make and /usr/share/mk files.

# Requirements

* GraphicsMagick
* FFTW3
* clFFT
* clover
